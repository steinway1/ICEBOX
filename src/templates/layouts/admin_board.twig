{% set THEME_ASSETS_FOLDER = './assets' %}
<!DOCTYPE html>
<html lang="en">

	<head>
		{% include 'page-components/admin-head.twig' %}
		{{ block('include_head') }}
	</head>


	<body class="admin_board">
		<main {% if mainClass is defined %} class="{{mainClass}}" {% endif %}> {% block content %}{% endblock %}
			</main>
		</body>
    {% include "admin/components/sidebar_board.twig" %}
		{% include "page-components/scripts.twig" %}
		{{ block('include_script') }}
	</html>
