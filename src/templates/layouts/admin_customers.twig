{% set THEME_ASSETS_FOLDER = './assets' %}
<!DOCTYPE html>
<html lang="en">

	<head>
		{% include 'page-components/admin-head.twig' %}
		{{ block('include_head') }}
	</head>


	<body {% if bodyClass is defined %} class="{{bodyClass}}"{% endif %}>
		{% include "admin/components/header.twig" %}

		<main {% if mainClass is defined %} class="{{mainClass}}" {% endif %}> {% block content %}{% endblock %}
			</main>

			{% include "admin/components/sidebar.twig" %}
			{% include "page-components/scripts.twig" %}
			{{ block('include_script') }}
			{% include "admin/components/back-backdrop.twig" %}
			{% include "admin/components/toolbar_store.twig" %}
			{% include "admin/components/swap-modal.twig" %}
		</body>

	</html>
