<div class="menu__list" data-menu-list="travelCases">
	<a href="/results/travel-cases" class="menu__item">
		<span>
			<strong>Shop All</strong>
		</span>
	</a>
  	{% for category in travel_cases_subcategories %}
		<a href="{{category.href}}" class="menu__item">
			<span>{{category.name}}</span>
		</a>
	{% endfor %}
</div>
