<div class="menu__list" data-menu-list="custom">
	<a href="/results/custom" class="menu__item">
		<span>
			<strong>Shop All</strong>
		</span>
	</a>
  	{% for category in custom_subcategories %}
		<a href="{{category.href}}" class="menu__item">
			<span>{{category.name}}</span>
		</a>
	{% endfor %}
</div>
